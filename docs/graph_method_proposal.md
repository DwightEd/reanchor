# 回看节点、证据选择与约束归属

更新：2026-09-11。本文是当前研究要求，取代此前的 G0/G1 训练方案。
历史方案保存在 Git 和带日期的笔记中，不作为当前实现说明。

## 研究目标

在真实自回归生成中，先定位模型重新读取、选择证据信息的**回看节点**，
再检查这些节点附近，来源内容与当前回答的对象、动作、限制条件如何组合。
目标是找到可复现的错误绑定模式，最终用于无幻觉标签训练的错误定位。

回看节点是计算事件，不是幻觉 token 标签。正确回答和错误回答都可能出现。
“定位决策窗口”与本研究的“定位回看节点”指同一目标，不另造概念。
不预先指定实体才能运行节点定位；具体实体/词语只用于事后解释和实例核验。
本轮先做缓存测量与对照，不训练检测器，不把 attention 的变化直接称为因果影响。

## 已确认的控制要求

原始预测为 z_t = LM(prompt, y_<t)，原模型概率为 softmax(z_t)。
logits 来自最终残差表示、归一化及词表投影，包含 attention、MLP 等计算的共同作用。
单条 attention 边不能解释全部 logit 变化。

当前 sampling.py 在 eval 模式前向：先得到原始 z，再用 softmax(z / temperature)
和 top-p 抽样；seed 只传给抽样用的 torch.Generator。
保存的 top_logits、chosen_logit、log_normalizer、logit_entropy 都属于未施加温度的 z。
float32 保存不代表前向是 float32；这批前向为 bfloat16。

- 固定模型、精度、前缀、位置和执行方式时，改变抽样 seed/温度不应直接改变当前原始 logits。
  用相同前缀实测数值误差，不能承诺跨设备/算子逐位一致。
- seed/温度会改变实际选择，进而改变后续前缀和原始 logits。相同 seed 不等于相同前缀。
- 同一前缀的首次不同输出，仍由同一个预测状态抽样得到；在这一步不能用前状态区分两条未来。
- 本轮固定已有 token ID 与采样缓存，仅导出原始概率；不重新采样、不把温度后分布当模型不确定性。
- 自动检查同一采样批次中跨 seed 的相同前缀：比较原始 top 候选、其 logits 与 log-normalizer。
  这只是已保存量的一致性检查，不等于比较了完整词表或所有激活。
- 同一回答中对齐语法结构/相同 token 的正确与错误段落，只是观察性对照，前缀仍不同。
  不能宣称已消除难度、位置、词形、模板、上下文长度等全部混杂。
- 后续需要补中间状态时，在固定 token 序列上做前向；不重新生成。
  对齐 KV-cache、dtype、attention 实现，先测重复前向误差，再解释条件差异。

top2_margin = z(top1) - z(top2)；chosen_margin 是实际 token 相对最强其他候选的差。
抽样 token 不一定是 top-1，chosen_margin 可以为负。top-5 概率使用完整词表 log-normalizer，
不能对 top-5 再 softmax。旧缓存没有完整 logits 熵时留空，不从 top-5 冒充恢复。

## 已有实例与应看的位置

以下来自 results/routes_20260911_075620_752，编号从 0 开始，均是事后实例。
观察窗口不是新生成回答的首错标注。

| 实例 | 内容问题或对照 | 回看与关系形成位置 | 后续属性位置 |
| --- | --- | --- | --- |
| 14375 / seed 0 | 把另一烹饪动作的 10–12 分钟附给洋葱 | t119 cook、t121 onions、t124 beer、t126 for、t127 空格 | t128 10、t131 12、t132 minutes |
| 同回答的正确局部描述 | 烤香肠 10–14 分钟 | t83 Cook、t85–87 bratwurst、t93 grill、t94 for、t95 空格 | t96 10、t99 14、t100 minutes |
| 同回答的正确局部描述 | 锅中 simmer 15–20 分钟 | t55 simmer、t56 for、t57 空格 | t58 15、t61 20、t62 minutes |
| 14315 / seed 2 | 头饰内容被泛化，Only the Inca 限制未保留 | t25 They、t33 and、t34 wore | t36–37 headdress；t46 句末另看 |
| 14315 / seed 3 | 及膝/及踝是有依据的局部描述 | t18 They、t19 wore、t26 with | t34 knee、t43 ankle |
| 14304 / seed 0 | 正常条目与主题转换 | t69–72：3. Star: | 后续正确解释 |

数字内容在 source 出现，不代表“当前对象—动作—时长”这个关系有 source 支持。
洋葱例子的 1–2 分钟指黄油炒制，也不能当作啤酒煮洋葱的正确替换答案。
头饰例子中，预测 wore 时 L22/H13 对 Only 的原始权重从 0.00394 升至 0.07227，
是该 head 在 source 上的最大权重；这说明注意分配变化，不能证明限制已被有效使用。

## Attention 熵与 head 选择

对每个 layer/head 单独在固定 passage token 集合 S 上归一化：

    p_t(j) = A_t(j) / sum_{k in S} A_t(k)
    H_t = -sum_j p_t(j) log2 p_t(j)

熵较高表示该分布更分散；不严格等于没有任何突出峰。多个 token 还可能属于同一证据片段，
因此 token 熵高也不自动等于存在多个互相竞争的语义来源。
H 只在同一来源集合、同一 head 和适当的前缀/词形对照下解释；原始 source 总量为零时无定义。
归一化会放大很小的权重，原始边权必须保留。

“正确和错误回看节点都比顺序输出更分散，而错误更甚”是待检验假设，尚非总体结论。
例如 L24/H27 在数字前空格：正确 1.53 bits、错误 4.43 bits；但错误的 12 处已降至 0.65 bits。
必须检查组织阶段到内容输出的过程，不能只对比被输出的实体。
head 间分歧也可能是合理分工；不能要求所有 heads 读取相同位置。

过去展示的 L24/H27、L15/H30 是根据实例中可读的强边事后挑出的说明性 heads，
不是已证实的检索/推理 head，也没有证明它们代表全模型或对错误有因果作用。
仅凭一个 token 的高 attention 不能确定 head 的功能，head 编号也没有先后功能含义。

本轮所有指定位置导出全部 layer/head，包括低权重、零权重和非最大边。
默认方法不增加功能 head 筛选模块：先逐 head 计算读取结构的变化，再等权汇总成节点读数。
这不同于先平均 attention 矩阵：两个 heads 分别读 A/B，与两个 heads 都均匀读 A/B，
平均矩阵相同，内部组织却不同。原始 layer/head 轴保留用于解释，不作为人工挑选检测器的入口。
若全 head 汇总实际丢失信号，再与独立参考样本上固定的距离分组比较；不能按四例的正误差挑 head。

## 已有回看方法与迁移边界

交大等机构的 [Attention Illuminates LLM Reasoning](https://arxiv.org/html/2510.13554v2)
已用 WAAD（截断的 attention 加权回看距离）和 FAI（后续位置读取当前 token 的程度）研究节律。
它按平均回看距离自动分组 heads；附录 B.6 的分组优势来自 RL credit assignment，
不能直接推出本任务也必须选 heads。该文不是自然 RAG 约束错绑检测的验证。

旧 graph 的 `experiments/reanchor_flow/attention_rhythm.py` 已实现这两个量，
对应提交 `c02a4c3a099daa07cd740742f19c1b5bbe8c053b`，本地保留在 `graph_reanchor_reference`。
后续复用公式和位置对齐，不恢复整套历史实验框架。
在线迁移只用当前及过去：不能用未来 FAI、下一步 WAAD 或整条回答的 head 排名决定当前事件。
WAAD 作为已有基线；它无法充分区分已经在远处的两个来源之间换读，不能独自覆盖所有回看。

## 节点量化与图建模：待验证的定义

现有 shift 是相邻 attention 分布的 TV 距离。它会把沿原文相邻 token 的顺序读取算成大变化，
只保留作对照，不以其峰值直接定义回看或错误。

下一步将节点定位收敛成一个待验证的读数 R_t，不额外拼接集中度和手工 head 选择。
令 a_(l,h,t) 是预测 y_t 的完整原始 attention 行，两步使用相同 token 位置坐标，
新增 key 在前一步补零。先计算一维 Wasserstein 距离，再与各 head 自己的过去比较：

    D_(l,h,t) = W1(a_(l,h,t), a_(l,h,t-1); cost(i,j)=|i-j|)
    R_t = mean_(l,h) max(0, D_(l,h,t) - median_(u in past window) D_(l,h,u))

W1 等于两行累计分布之差的绝对值之和，单位是 token 位置；行概率只校正保存精度造成的质量误差。
这利用 token 链上的距离：相邻子词的峰移动代价小，远距离换读代价大，
分散到集中或集中到分散也可产生变化，不必先识别实体。熵只辅助解释变化形态。
R_t 是读取重组候选分数；正负方向、头间分工以及正确性不能从这个标量单独恢复。
两次分布恰好不变时它为零，不能保证发现所有决策；稳定的错误读取也可能不报警。

这是把“回看节律”适配到来源分布变化的候选定义，尚未实现或验证，不宣称公式本身有方法新颖性。
完整行也含指令和特殊 token，必须检查这些变化造成的假候选；不用低质量 source 行归一化来放大噪声。
过去窗口、起始不足窗口的处理与候选门槛须在独立参考样本固定，不在这四例上调到符合预期。
比较 WAAD、原 TV 与 R_t 的节点覆盖；纳入连续读取、子词、标点、正常条目转换和没有事件的错误。
检测不使用未来；事后窗口图可以看后续。定位事件仍不需要训练 GNN。

图中的回看节点需要保留 source、生成历史、当前 query 的真实 token 身份与 layer/head。
检查“当前对象/动作指向的来源”和“属性/时长指向的来源”是否保留了适用关系。
source 邻接本身不能判断语义归属；存在路径、复制成功、多个 heads 一致均不等于事实正确。
attention 路径乘积也只是读取代理，不能忽略 MLP、value、残差与归一化后声称得到完整因果流。

## 图是否必要：用关系信息决定

当前 graph 的 `route_graph/operator.py::PathEncoder.encode` 确实沿有序层传播节点信号，
使用 `(I+A)/2`，并比较真实端点与条件置换期望，包含 source 经 history 到 query 的路径。
它不是只有图名的特征表；但读出的是候选方向的端点对齐，尚未验证能恢复约束的适用对象。
reanchor 的 `routes.py` 目前则是分布统计和具体边读取，没有上述路径传播。

本研究优先检验的错误形态是：**属性内容被成功复制，但对象—动作—属性的适用关系没有保持。**
例如洋葱与时长都能在 source 找到，各自的强峰仍可组成没有依据的回答。
需要比较回看前正在描述的对象/动作所承接的来源，与属性选择所承接的来源之间的关系；
不能要求不同角色的 heads 都读同一个 token，也不能把跨 passage 当作错误。
运行时保留全部 token 的上下文表示与读取路径，实体名称只用于事后核验。

单纯无内容的拓扑、谱熵或邻接距离不能唯一判断约束归属。
交换同构图上两个对象的内容，图谱可以不变而答案正确性改变。
因此图算子需要作用于带上下文内容的节点/消息；源文中的关系如何编码和读出仍需验证。
可以先复用冻结模型已有表示；若另训关系读出，则属于新增训练，不能继续称为完全免训练。
无监督学习到的常见模式也不自动等于正确约束。

是否保留图，用同样的 source 拆分、同样的事件窗口、同样的可用状态和读出容量比较：
margin/熵/回看距离、无边的节点读出、保留真实连接的图读出。
另将分析图的端点在固定因果方向、来源和距离条件内置换，检查实际连接的增量；
这项比较无需干预生成模型，但置换也可能改变分布，不能把性能下降单独当作机制证据。
若图不能在独立来源上增加首错定位能力，或仅检测连续错误的后半段，就不保留复杂图模块。

## 当前实例支持到哪里

同一回答 14375 / seed 0 的原始 top-2 logit margin：

| 预测位置 | 有依据：simmer 15–20 | 有依据：grill 10–14 | 无依据：onions 10–12 |
| --- | ---: | ---: | ---: |
| for | 9.125 | 1.750 | 0.125 |
| 首个数字前空格 | 10.000 | 12.250 | 0.125 |
| 首个数字 | 3.625 | 3.125 | 1.375 |

数值来自 [tokens.csv](../results/routes_20260911_075620_752/00012/tokens.csv)。
L24/H27 在正确/错误数字前空格的 source 熵分别为 1.528/4.434 bits；
但错误的 12 处熵已降至 0.649 bits，对源文 12 的 attention 为 0.8984。
L15/H30 在预测 onions 时峰指向 brat，在预测 beer 时峰指向 butter。
这些是 [reading.csv](../results/routes_20260911_075620_752/00012/reading.csv) 中事后选择的例内读数。

已有“组织阶段不确定、随后集中读取不匹配内容”的线索，尚无总体显著性、自动关系读出或因果结论。
不能将连续 token 当作独立样本扩大证据，也不能由 top-2 margin 推算完整 logits 熵。

新同步的 `results/routes_20260911_091806_237/decisions` 是同批生成的进一步导出，不是新增独立样本：

- [same_prefix.csv](../results/routes_20260911_091806_237/decisions/same_prefix.csv) 的 138 条跨 trace
  同前缀比较，保存的 top 候选 ID 全一致，候选 logits 和 log-normalizer 的最大差均为 0。
  本批温度固定；这不等于检验了改变温度后的全部状态，也不是完整词表比较。
- [candidates.csv](../results/routes_20260911_091806_237/decisions/candidates.csv)：
  t126 的原始概率 on=0.3734、for=0.3295、over=0.1070，实际抽到次优候选 for；
  t128 的 10=0.7919、1=0.2002；t131 的 12=0.9952。
  因而观察到的是由不同续写选项走向高置信数值延续。10/1 对应原文不同数字开头，
  不能仅凭下一 token 候选宣称模型已规划出两条完整时长；1–2 也不是当前动作的正确替换。
- [reads.csv](../results/routes_20260911_091806_237/decisions/reads.csv)：
  t128，L7/H9 对历史 onions(t121) 为 0.5078，而 L24/H27 对 source 10(key73) 为 0.7422。
  正确 t96，L7/H9 对历史 bratwurst 的末子词(t87) 为 0.4102。
  两处都存在明显的对象回读，不能用“是否读了对象”区分正误；跨层两条强边尚不证明有效绑定或完整因果路径。
- 同一导出的所有 1024 个 layer/head 证实：t34 预测 wore 时对 Only 的最大原始权重确为
  0.07227（L22/H13），次高 0.06177（L20/H20）。无需再依靠只存峰值的 CSV 推断上界。
  [tokens.csv](../results/routes_20260911_091806_237/decisions/tokens.csv) 还显示 wore 的原始概率
  仅 0.0305，未进入保存的 top-5；不能把抽中的低概率词当作 top-1 决策。

## 本轮实现范围与验收

执行路径：main.py decisions -> DecisionInspection.run() -> 具体 CSV。
保持现有采样与 routes 算法，先完成已同意的两个检查，并增加相同前缀的数值审计。

- 读取指定实例的固定预测窗口，核对窗口的实际 token 文本，防止误用另一批生成的编号。
- 导出每个保存的 top 候选的 ID、文本、原始 logit、完整词表概率，以及实际选择概率/margin。
- 对指定 prompt key 和生成历史 key，导出所有层、所有 heads 的真实边权与前一步权重。
  新出现的 key 没有可比前值时留空；不可见的 key 不导出。query 自身的 distance=0，不叫回看。
- t 行 query=P+t-1，预测 response token t。若追踪 response token u 自身的读取，
  对应的是行 u+1，而不是预测它的行 u；本轮不把两者混用，也不把文本邻接冒充神经路径。
- 自动导出批次内相同前缀的原始候选数值差异；不能用不同实际抽样 token 的 chosen_logit 差异
  误报同一预测状态不一致。相同前缀记录是重复测量，不是独立统计样本。
- 脚本只用现有 NPZ，在 CPU 运行，有进度条；保存数字和输入依据，不生成 report，不改原始采样。

验证通过项目公共入口 main.py 的 CSV 输出进行：原始概率不受温度缩放、抽样 token 可非 top-1、
前后时序与新 key 可见性正确、低权重非最大边仍导出、相同前缀只比较相同预测任务。
使用微型真实 Llama 前向检查不同 seed/温度对同一前缀的原始状态不产生直接影响。
合成测试用于程序正确性，不作为机制实验结果。

当前本地已收到上述四例的 top 候选、指定历史/来源边和批次同前缀核验 CSV；完整 NPZ 仍在服务器。
旧采样缓存没有保存完整 logits 熵，新导出的该列仍为空；这些结果也不足以重建完整跨层消息图。

## 证据成立的顺序

1. 先验证测量与采样控制，再确认回看节点相较顺序输出的差异。
2. 在回看窗口内部对照正确/错误的来源选择和约束归属；不预设错误一定更高熵。
3. 有稳定结果后，在独立 source 上冻结算法评估；新生成回答需要自己的错误标注。
4. 最后用有限、固定前缀的干预检查机制，不能用观察相关性替代因果结论。

## 参考依据

- [Transformers 生成参数](https://huggingface.co/docs/transformers/main_classes/text_generation)：
  temperature/top-p 作用于抽样分布。
- [PyTorch 可复现性](https://docs.pytorch.org/docs/stable/notes/randomness.html)：
  相同 seed 不保证跨平台/版本/算子逐位一致。
- [Retrieval Head](https://arxiv.org/abs/2404.15574)：检索 heads 稀疏且随上下文激活。
- [Attention is Not Only a Weight](https://aclanthology.org/2020.emnlp-main.574/)：
  attention 权重不能替代变换后的 value 贡献。
- [How do Language Models Bind Entities in Context?](https://arxiv.org/abs/2310.17191)：
  绑定信息是可研究的内部表示对象；本文的自然错误实例尚未验证同一机制。
- [Attention Illuminates LLM Reasoning](https://arxiv.org/html/2510.13554v2)：
  Li 等，2025/2026 预印本；回看/anchor 节律作为已有基线，不是本研究的创新主张。
- [A retrieval conditioned rebinding circuit for dynamic entity tracking in large language models](https://arxiv.org/html/2606.08644v1)：
  Oh、Demberg，2026 预印本；受控状态追踪中读出时重绑定，不能直接推广为自然幻觉机制。

本次新增核读的节律、重绑定及上述实体绑定论文，均经 arXiv 元数据核验（verified via arxiv）；
方法与适用边界另对照原文，文献存在性核验不等于验证论文结论。
